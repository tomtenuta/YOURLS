---
description: Apply during configuration or environment variable setup to ensure secure secret handling, 12-factor compliance, and proper documentation.
alwaysApply: false
---
title: Configuration & Secrets
description: Required env vars and secret handling.
rule_type: Always
globs:
  - "infra/**"
  - "src/**"
alwaysApply: true
tags: [config, secrets]
---

# Env Contracts (examples, not code)
- SHORTENER_BASE_URL, ADMIN_ORIGIN
- DB_DSN, REDIS_URL
- RATE_LIMITS_CREATE, RATE_LIMITS_RESOLVE
- ANALYTICS_RETENTION_DAYS
- WEBHOOK_SECRET, API_KEY_HASH_SALT

# Rules
- No defaults for secrets; fail at boot if missing.
- Document every env var in `/docs/config.md`.
